<UserControl x:Class="KPS.PetroUI.Shell.Controls.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <StatusBar Background="{DynamicResource MahApps.Brushes.Accent}" Foreground="White">
        <StatusBarItem>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Database" Width="14" Height="14" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ConnectionStatus}" Margin="6,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </StatusBarItem>
        <Separator Background="White" Opacity="0.3"/>
        <StatusBarItem>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Account" Width="14" Height="14" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentUserName}" Margin="6,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </StatusBarItem>
        <StatusBarItem HorizontalAlignment="Right">
            <TextBlock Text="{Binding Clock}" VerticalAlignment="Center"/>
        </StatusBarItem>
    </StatusBar>
</UserControl>
