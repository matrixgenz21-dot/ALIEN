<mah:MetroWindow x:Class="KPS.PetroUI.Shell.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="KPS PetroUI — Login"
    Width="420"
    Height="480"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    ShowTitleBar="True"
    ShowCloseButton="True"
    ShowMinButton="False"
    ShowMaxRestoreButton="False"
    BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
    BorderThickness="1">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,8,0,24">
            <TextBlock Text="KPS PetroUI" FontSize="28" FontWeight="Light" HorizontalAlignment="Center"/>
            <TextBlock Text="Khalifa Petroleum Services" FontSize="12" Opacity="0.6" HorizontalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBox x:Name="UserNameBox"
                     Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Username"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Margin="0,0,0,16"/>
            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,12"/>
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource ValidationErrorBrush}"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    HorizontalAlignment="Left"/>
            <Button Grid.Column="1"
                    x:Name="LoginButton"
                    Content="Sign In"
                    IsDefault="True"
                    Click="LoginButton_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Grid>
</mah:MetroWindow>
